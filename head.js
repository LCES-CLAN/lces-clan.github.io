// Shared <head> content — loaded on every page
(function() {
  // Favicon only — fonts and stylesheet are in static HTML for instant loading
  var favicon = document.createElement('link');
  favicon.rel = 'icon';
  favicon.href = 't38156.jpg';
  document.head.appendChild(favicon);
})();
